spring:
  datasource:
    url: jdbc:mysql://localhost:3306/movie_web?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  jackson:
    serialization:
      write-dates-as-timestamps: false

server:
  port: 8080

security:
  jwt:
    secret: change-this-to-a-very-long-random-secret-key-in-production
    expiration-ms: 86400000   # 24 hours
  password-salt: i dont know

logging:
  level:
    com.movieweb: DEBUG
    org.springframework.security: INFO
